import React from "react";
import { Outlet } from "react-router-dom";

const CommandStaffSupervisor = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default CommandStaffSupervisor;
