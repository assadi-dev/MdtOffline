import React from "react";

const EffectifsReducer = (state, action) => {
  const { type, payload } = action;
  switch (type) {
    default:
      return state;
      break;
  }
};

export default EffectifsReducer;
